# SynthSpeak — Python Backend Dependencies
# Use opencv-python-headless on cloud servers (no display required)

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
httpx>=0.27.0

# AI / ML
mediapipe==0.10.9
numpy>=1.26.0
scipy>=1.13.0
opencv-python-headless>=4.9.0  # headless: no GUI, safe for cloud

# Speech
requests>=2.31.0  # Deepgram REST calls

# NLP / Embeddings (NVIDIA NIM)
# (called via httpx REST — no extra package needed)

# PDF / DOCX resume parsing (optional features)
PyPDF2>=3.0.0
python-docx>=1.1.0

# Pydantic (FastAPI dependency)
pydantic>=2.0.0
